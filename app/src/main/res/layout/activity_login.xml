<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
android:background="@mipmap/login_background"
    android:id="@+id/container"
              android:layout_width="match_parent"
              android:layout_height="match_parent"  android:weightSum="1">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content" android:singleLine="true"
        android:id="@+id/user"
        android:layout_toRightOf="@+id/imageView3" android:layout_alignParentBottom="false"
        android:layout_marginLeft="30dp" android:layout_marginRight="30dp"
        android:textColor="@color/white" android:hint="请输入手机号"
        android:imeOptions="actionNext"
        android:layout_marginTop="270dp"
        android:text="admin"
        android:textSize="16sp"
        android:drawableLeft="@mipmap/mobile"
        android:drawablePadding="7dp"
        android:textColorHint="@android:color/darker_gray"
        android:layout_marginBottom="20dp"
        android:layout_centerVertical="true" />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:text="123456"
        android:textColorHint="@android:color/darker_gray"
        android:ems="10"
        android:drawableLeft="@mipmap/lock"
        android:drawablePadding="7dp"
        android:id="@+id/password" android:layout_gravity="center_horizontal"
        android:layout_toRightOf="@+id/imageView4" android:layout_centerVertical="true" android:hint="请输入密码"
        android:layout_marginRight="30dp"
        android:textSize="16sp"
        android:layout_marginLeft="30dp" android:textColor="#fff" android:imeOptions="actionDone"
        android:maxLines="1" />
    <Button
            android:layout_width="match_parent"
            android:layout_height="43dp"
            android:text="登    录"
            android:textColor="@color/white"
            android:id="@+id/login" android:background="@drawable/button_selector"
            android:layout_gravity="center_horizontal" android:layout_marginTop="40dp"
            android:layout_marginRight="45dp" android:layout_marginLeft="45dp" android:layout_marginBottom="10dp"/>

</LinearLayout>